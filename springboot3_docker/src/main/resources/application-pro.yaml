# ************************************************************分割线************************************************************

server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql://docker_mysql:3306/untitled?useSSL=false&characterEncoding=utf-8
    username: root
    password: 2468
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database: mysql
    show-sql: true
